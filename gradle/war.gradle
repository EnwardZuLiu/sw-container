apply plugin: "war"

bootWar {
    mainClassName = "com.safeweb.SwContainersApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
